|| Пример 3: конфигурация сбора данных (IoT / аналитика)

device_id = 'sensor-42';

sampling_hz = 50.0;      || частота дискретизации
window_sec = 10.0;       || окно агрегации
window_samples = ^(* sampling_hz window_sec);

thresholds = [0.15; 0.05; 0.10; 0.20];
thresholds_sorted = ^(sort() thresholds);
