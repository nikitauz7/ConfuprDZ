|| Пример 2: конфигурация баланса для игры (game design)

weapon_name = 'plasma_rifle';

base_damage = 18.0;
crit_mult = 1.75;
crit_damage = ^(* base_damage crit_mult);

mag_size = 30.0;
reload_sec = 2.4;

|| Таблица весов лута (меньше — реже)
loot_weights = [50.0; 5.0; 15.0; 30.0];
loot_weights_sorted = ^(sort() loot_weights);

<# 
  Можно хранить “коды” предметов строками:
  (языку словари не требуются — просто отдельные константы)
#>
loot_common = 'medkit';
loot_rare = 'legendary_core';
